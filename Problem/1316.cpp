#include <iostream>
#include <string>
#include <vector>
using namespace std;
/*
// 그룹 단어 체커
void p1316(){
	int N;
	string input;
	char compare; // 문자 비교 대상
	int check; // 같은 문자 있는지 확인해줄놈

	// happy  h 가지고 계속 체크 a 가지고 계속 체크 근데 바로 옆에 있는 거 말고,, 음 ,,
	/*그러면 h가 첫문자니까 첫문자 가지고 돌려 돌리다가 없네? 그러면 통과
	그다음 a 임시저장하고 a 가지고 돌려 돌리다가 없네? 그러면 통과
	p 임시저장하고 돌려 돌리다가 바로 옆에 발견해 어 근데 바로 옆이니까 ㄱㅊ 또 돌리다가 어 바로옆에네 ? ㄱㅊ 근데 
	if(temp == input[k]) 어 어떡ㅎ자ㅣ 
	하나씩 빼ㅜ저야하냐?

	for (int i = 0; i < N; i++) {
		cin >> input;
		compare =input[0];
		for (int k = 0; k < input.size(); k++) {
			if (input[k] == compare)
				check++;
				continue;
			else
				compare = input[k+1];
		}
	}


	int count = 0;


}*/